# Patched Reality Node demo

## Install
```
npm install
npm install -g nodemon (optional)
```

## Run locally
```
npm start
npm run nodemon (if you have nodemon installed)
```
Open in your browser: [http://localhost:3000](http://localhost:3000)
